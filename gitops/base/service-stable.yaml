apiVersion: v1
kind: Service
metadata:
  name: regulated-ai-mlops-stable
  labels:
    app.kubernetes.io/name: regulated-ai-mlops
    delivery-track: stable
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: regulated-ai-mlops
    app.kubernetes.io/component: inference-api
  ports:
    - name: http
      port: 80
      targetPort: http
